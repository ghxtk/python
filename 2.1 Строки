//Добавь звёзды!
//Дано слово. Добавить к нему в начала и конце столько звёздочек, сколько букв в этом слове.

s = input()
print("*" * len(s) + s + "*" * len(s))

//Слово в столбик в обратном порядке
//Дано слово, состоящее из латинских букв. 
//Вывести его "столбиком" в обратном порядке, начиная с последней буквы.

s = input()
for i in range(len(s)):
    print(s[len(s) - i - 1])
    
//Буквы на четных местах в обратном порядке
//Дано слово s1. 
//Получить слово s2, образованное четными буквами слова s1, записанными в обратном порядке.

s = input()
for i in range(len(s)):
    if (i + 1) % 2 == 0:
        if (len(s) % 2 != 0):
            print(s[len(s) - i - 1], end = '')
        else:
            print(s[len(s) - i], end='')
            
//Строки. Задача 19
//Дана строка S. Проверьте, является ли она симметричной.      
 
 s = input()
count = 0
for i in range(len(s)//2):
    if s[i] == s[-i - 1]:
        count += 1
if count == len(s) // 2:
    print(1)
else:
    print(0)

//Строки. Задача 3
//Дана строка S. Удалите из неe среднюю букву, если длина строки нечeтная, или две средних в противном случае.

s = input()
if len(s) % 2 == 0:
    print(s[:len(s) // 2 - 1] + s[len(s) // 2 + 1:])
else:
    print(s[:len(s) // 2] + s[len(s) // 2 + 1:])
    
//Сумма цифр
//Дана строка, в которой имеются цифры. Найти их сумму.    
    
print(sum(map(int, filter(str.isdigit, ''.join(input())))))  
